services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: foxygen_vibe
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
